apiVersion: 1

providers:
  - name: earthwatch
    folder: Earthwatch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
